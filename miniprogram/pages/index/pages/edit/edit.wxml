<view class="page">
  <view class="page-header">
    <view class="button-submit" bindtap="submit">发布</view>
  </view>
  <view class="form">
    <view class="form-item">
      <view class="form-label">游戏</view>
      <view class="form-value">
        <picker bindchange="bindTypeChange" value="{{gameTypeIndex}}" range="{{gameTypeList}}">
          <view class="picker">
            {{gameTypeList[gameTypeIndex]}}
          </view>
        </picker>
      </view>
    </view>
    <view class="form-item">
      <view class="form-label">人数</view>
      <view class="form-value">
        <picker bindchange="bindMaxNumChange" value="{{maxNumIndex}}" range="{{maxNumList}}">
          <view class="picker">
            {{maxNumList[maxNumIndex]}}
          </view>
        </picker>
      </view>
    </view>
    <view class="form-item">
      <view class="form-label">开始时间</view>
      <view class="form-value">
        <picker mode="time" value="{{startTime}}" start="09:01" end="23:59" bindchange="bindStartTimeChange">
          <view class="picker">
            {{startTime}}
          </view>
        </picker>
      </view>
    </view>
  </view>
</view>